language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t kevkevin/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true kevkevin/docker-react npm run test -- --coverage